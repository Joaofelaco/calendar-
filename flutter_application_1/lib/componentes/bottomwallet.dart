import 'package:flutter/material.dart';

class Vagas extends StatelessWidget {
  const Vagas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text('myapp')),
    );
  }
}
